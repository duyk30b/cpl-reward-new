export enum Environment {
  LOCAL = 'local',
  DEV = 'dev',
  STAGING = 'stg-v2',
  PROD = 'prod',
}

export enum Role {
  USER = 'user',
  GUESS = 'guess',
}

export enum SortType {
  ASC = 'ASC',
  DESC = 'DESC',
}
