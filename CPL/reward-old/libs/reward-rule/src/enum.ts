export enum REWARD_RULE_APPLY_FOR {
  CAMPAIGN = 'campaign',
  MISSION = 'mission',
}

export enum REWARD_RULE_WALLET {
  BALANCE = 'balance',
  CASHBACK = 'cashback',
  DIVIDEND = 'dividend',
  REWARD = 'reward',
}
