export enum Environment {
  LOCAL = 'local',
  DEV = 'dev',
  STAGING = 'staging',
  PROD = 'prod',
}

export enum EService {
  AUTH = 'auth',
}

export enum Role {
  USER = 'user',
  GUESS = 'guess',
}

export enum CheckBoxValue {
  ON = 1,
  OFF = 0,
}

export enum TradeType {
  BUY = 'buy',
  SELL = 'sell',
}
