export enum MissionUserLogStatus {
  // IGNORE = 0,
  NEED_TO_RESOLVE = 1,
  RESOLVED = 2,
  RETRYING = 3,
}

export enum CAMPAIGN_TYPE {
  DEFAULT = 0,
  ORDER = 3,
}

export enum CAMPAIGN_STATUS {
  RUNNING = 1,
  OUT_OF_BUDGET = 2,
  ENDED = 3,
  COMING_SOON = 4,
}
